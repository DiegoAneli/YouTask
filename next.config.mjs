
  
  const nextConfig = {
    env: {
      NEXT_PUBLIC_API_URL: 'http://localhost:5000/api',
    },
    reactStrictMode: true,
    // Aggiungi altre configurazioni qui se necessario
  };
  
  export default nextConfig;